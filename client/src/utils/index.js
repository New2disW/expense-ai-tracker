// src/utils/index.js

export function createPageUrl(pageName) {
  // This is a placeholder function. In a real-world app, this
  // would handle proper routing based on your framework (e.g., React Router).
  // For this project, it simply returns the page name as the URL path.
  // For example, createPageUrl("AddExpense") would return "/AddExpense".
  return `/${pageName}`;
}